services:
  relay:
    build: .
    restart: unless-stopped
    ports:
      - "127.0.0.1:8090:8090"   # Caddy reverse proxies this
    env_file: .env
    healthcheck:
      test: ["CMD", "wget", "-qO-", "http://localhost:8090/health"]
      interval: 30s
      timeout: 5s
      retries: 3
