services:
  relay:
    build: .
    restart: unless-stopped
    ports:
      - "80:8090"
    env_file: .env
    healthcheck:
      test: ["CMD", "wget", "-qO-", "http://localhost:8090/health"]
      interval: 30s
      timeout: 5s
      retries: 3
